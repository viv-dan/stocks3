package stockscontroller;

/**
 * The interface specifies the controller features.
 */
public interface Controller {

  /**
   * The method helps run the controller object that communicates with Stocks model for retrieving
   * the data and the Stocks view for displaying the data.
   * UPDATES : Added new switch cases in the design to incorporate new features.
   */
  void startProgram();
}
